# Core
sqlalchemy[asyncio]>=2.0,<3
asyncpg>=0.29
alembic>=1.13
pydantic>=2
pydantic-settings>=2
python-dotenv>=1.0

# Workflow (Phase 6)
hatchet-sdk>=0.40

# HTTP + parsing (Phase 2–3)
httpx[http2]>=0.27
selectolax>=0.3
beautifulsoup4>=4.12
lxml>=5.0
feedparser>=6.0
playwright>=1.40

# Content extraction (Phase 3)
trafilatura>=1.9
readability-lxml>=0.8

# PDF (Phase 4)
pdfplumber>=0.11
pytesseract>=0.3
Pillow>=10.0
pdf2image>=1.17

# Language detection (Phase 3)
lingua-language-detector>=2.0

# Utils
loguru>=0.7

# Dev / seed
psycopg2-binary>=2.9
